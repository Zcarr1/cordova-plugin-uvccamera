repositories {
    flatDir {
        dirs 'src/main/libs'
    }
}

dependencies {
  implementation (name:'libuvccamera-release', ext:'aar')
  implementation (name:'usbCameraCommon-release', ext:'aar')
}