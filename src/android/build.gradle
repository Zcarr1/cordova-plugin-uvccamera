allprojects {
  repositories {
    flatDir {
      dirs 'src/main/libs'
    }
  }
}

dependencies {
  implementation (name:'libuvccamera', ext:'aar')
  implementation (name:'usbCameraCommon', ext:'aar')
}